import type { LoomspaceState } from './types';

export const sampleState: LoomspaceState = {
  workspaceId: 'loomspace-demo',
  title: 'Loomspace',
  threads: [],
  selectedThreadId: null,
  selectedNodeId: null,
  densityOverlay: true,
  panX: 0,
  panY: 0,
  zoom: 1,
  version: 1,
};
